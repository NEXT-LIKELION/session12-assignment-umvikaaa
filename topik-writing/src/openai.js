export async function correctGrammar(text) {
    const apiKey = import.meta.env.VITE_OPENAI_API_KEY;

    const response = await fetch("https://api.openai.com/v1/chat/completions", {
        method: "POST",
        headers: {
            Authorization: `Bearer ${apiKey}`,
            "Content-Type": "application/json",
        },
        body: JSON.stringify({
            model: "gpt-4.1",
            messages: [
                {
                    role: "system",
                    content: `
  Ты учитель корейского языка. Исправляй предложения на реальный корейский, как можно более натурально и естественно. Если же предложение отличное, без ошибок просто скажи что все классно '잘했어요'.
  
  1. Исправь грамматические ошибки в следующем тексте.
  2. Покажи исправленную версию текста. Перед исправленным текстом поставь эмодзи 🐯.
  3. После исправленного текста объясни все исправления на русском языке.
  4. Причины исправления напиши в виде нумерованного списка (1., 2., 3. ...), чтобы каждая причина была отдельно, а не в одном абзаце.
  5. Не объясняй исправления слишком коротко. Приводи краткие пояснения и правила.
  
  Пример формата вывода (После каждой причины делай enter и переходи на новый абзац):
  
  🐯 Исправленная версия: [исправленный текст]
  
  Причины исправления:
  1. [объяснение первой ошибки]
  2. [объяснение второй ошибки]
  3. [объяснение третьей ошибки]
            `.trim(),
                },
                { role: "user", content: text },
            ],
            temperature: 0.5, // 자연스러운 답변을 위해 살짝 부드럽게
            max_tokens: 1000, // 넉넉하게 받아올 수 있게 설정
        }),
    });

    const data = await response.json();
    return data.choices[0].message.content.trim();
}
